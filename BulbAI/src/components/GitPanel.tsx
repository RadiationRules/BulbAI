import React from 'react';

const GitPanel = () => (
  <div className="git-panel">
    <h3>Git Version Control</h3>
    <p>Basic git actions and project history will be shown here. (For full git, use local clone.)</p>
    {/* Implement git commit, history, and fork/duplicate UI here */}
  </div>
);

export default GitPanel;
